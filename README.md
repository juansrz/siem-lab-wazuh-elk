# Wazuh + Elastic SIEM Lab (URJC-Inteligencia de la seguridad)
Laboratorio con **Wazuh + Elastic** y **Suricata**: reglas (ICMP/SSH/SQLi/DNS), DVWA (Docker), integración con VirusTotal y simulaciones (Nmap/SQLi/DNS/reverse shell/SSH brute force).

📄 **Informe completo**: `docs/Practica 2. Inteligencia de la Seguridad.pdf`

**Temas:** wazuh, siem, elk, suricata, dvwa, virustotal, nmap, sqli, dns, scapy, blue-team  
**Estado:** Demo académica reproducible (config y scripts se añadirán más adelante).
